<!--
Title convention (lowercase prefix, same as issues):
  feat: …    fix: …    ci: …    docs: …    chore: …

Examples:
  feat: surface dev_skip_percent in /health
  ci: publish container image to GHCR on release
  docs: document OfflineService in AGENTS.md
-->

## Summary

What changes, and why. One short paragraph or a few bullets. Lead with the **why** — the diff already shows the what.

## Test plan

- [ ] Integration tests pass: `cd tests/integration && uv run pytest`
- [ ] <PR-specific check>
- [ ] <PR-specific check>

## Notes / links (optional)

- Linked discussion / design doc:
- Follow-up tickets (out of scope here):

Closes #
